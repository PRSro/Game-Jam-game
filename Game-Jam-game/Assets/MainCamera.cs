using UnityEngine;

public class MainCamera : MonoBehaviour
{
    // Camera zoom/pan is now handled by ZoomController on the UI canvas.
    // This script is intentionally empty to avoid input conflicts.
    // Do not add Input polling here; it blocks editor tab switching.
}
